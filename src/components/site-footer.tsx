export function SiteFooter() {
  return (
    <footer className="mx-auto flex w-full max-w-6xl flex-col gap-4 border-t border-line/70 px-6 py-10 text-sm text-muted sm:px-10 lg:px-16 lg:flex-row lg:items-center lg:justify-between">
      <p>
        Seed dataset transformed from the original curated list into a typed
        registry foundation for a full Next.js product.
      </p>
      <p>MIT, repo-backed data, and designed to stay free.</p>
    </footer>
  );
}
